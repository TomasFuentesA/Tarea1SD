syntax = 'proto3';

message Item {
    float price = 1;
    float count = 2;
    string category = 3;
}

message Response {
    repeated Item items = 1;
}

message GetItemsRequest {
    string name = 1;
}

service ItemService {
    rpc GetItem(GetItemsRequest) returns (Item) {}
}
